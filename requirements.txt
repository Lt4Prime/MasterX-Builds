# MasterX 0.9.2 - Python dependencies
# Compatible with Windows and macOS

# Core / UI
Pillow>=10.0
keyring>=25.0
tkinterdnd2>=0.4.3

# AI providers
google-genai>=1.0
openai>=1.0

# OCR
pytesseract>=0.3.13

# Background tray / global shortcuts
pystray>=0.19.5
pynput>=1.7.7

# Windows-only
keyboard>=0.13.5; platform_system == "Windows"
pywin32>=306; platform_system == "Windows"

# Build
pyinstaller>=6.0
